Install CLAM from the Python package index with:

 $ pip3 install clam

Install the webservice:

 $ python3 setup.py install

You may need to use sudo for global installation. We recommend the use of a Python virtual environment. Only Python 3 and above are supported.